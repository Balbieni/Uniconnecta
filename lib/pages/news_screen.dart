import 'package:flutter/material.dart';

class NewsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Busca Universidades',
      theme: ThemeData(
        primarySwatch: Colors.purple,
      ),
    );
  }
}
